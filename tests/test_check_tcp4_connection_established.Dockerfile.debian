FROM debian:stable-slim

ENV VERBOSE_LOGGING=true

SHELL ["/bin/bash", "-o", "pipefail", "-c"]

COPY / /workdir/

RUN set -x && \
    apt-get update && \
    apt-get install —-no-install-recommends -y \
      ncat \
      net-tools \
      && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/*
    
ENTRYPOINT [ "nc", "-vv", "redis", "6379" ]
